{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w signify-ts run build",
    "test": "npm -ws test",
    "lint": "npm -ws run lint",
    "format": "npm -ws run format",
    "format:check": "npm -ws run format:check",
    "test:integration": "WITNESS_DEMO_HOSTNAME=witness-demo VLEI_SERVER_HOSTNAME=vlei-server npm -w signify-ts run test:integration"
  }
}
