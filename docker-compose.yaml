services:
  keria:
    build:
      context: .
      dockerfile: ./images/keria.dockerfile
    healthcheck:
      test: curl http://localhost:3902/health
      interval: 5s
      timeout: 2s
      retries: 10
